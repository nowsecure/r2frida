{
  "name": "r2io-frida-agent",
  "version": "1.0.0",
  "description": "Agent for frida:// io plugin",
  "private": true,
  "main": "agent/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "frida-compile agent -o _agent.js",
    "watch": "frida-compile agent -o _agent.js -w"
  },
  "license": "LGPL-3.0",
  "dependencies": {
    "frida-compile": "^1.2.0",
    "mjolner": "^1.0.4"
  }
}
